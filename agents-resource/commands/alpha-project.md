Call the mcp__ALPHA__project_init tool to initialise the current directory as a project using the global ~/.alpha-ai installation.

Usage: /alpha-project --init

This creates α/config.json, .mcp.json, and per-project data dirs in ~/.alpha-ai.
Requires ~/.alpha-ai to be installed first (run install.sh --global from any project).
Safe to re-run.
