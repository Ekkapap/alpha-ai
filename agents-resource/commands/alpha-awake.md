Call `mcp__ALPHA__awake` with optional `path: "$ARGUMENTS"` to restore session context.

- No args: full graph overview + previous session summary
- With path: focused context for that path (sketch results + previous session summary) — use this to reduce tokens when working on a specific area
