## What does this PR do?

<!-- อธิบายสั้นๆ ว่าเปลี่ยนแปลงอะไร / Brief description of changes -->

## Type

- [ ] `add` — new skill / feature
- [ ] `fix` — bug fix
- [ ] `update` — update existing
- [ ] `docs` — documentation only

## Test

```bash
rm -rf /tmp/test-alpha && mkdir -p /tmp/test-alpha
bash install.sh /tmp/test-alpha
```

- [ ] Tested locally with the command above
- [ ] No secrets or personal data included
